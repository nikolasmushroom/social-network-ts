import {ActionTypes, NavigationType} from "./store";
const initialState : NavigationType = {}
export const sidebarReducer = (state = initialState, action : ActionTypes) : NavigationType => {
    return state
}